# Copy to .env and edit. All keys are optional — defaults are sensible.

# MODEL_WEIGHTS=weights/unet_resnet34_ttpla.pth
# DEVICE=cuda
# TILE_SIZE=512
# TILE_OVERLAP=64
# CONFIDENCE_THRESHOLD=0.5
# MAX_UPLOAD_MB=10
# CORS_ORIGINS=["https://ktp.yourdomain.dev"]
