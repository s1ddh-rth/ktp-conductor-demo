# ─── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
.eggs/
build/
dist/
develop-eggs/
.installed.cfg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
htmlcov/
.cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyre/
.pytype/
.dmypy.json
dmypy.json
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/

# ─── Virtual environments ──────────────────────────────────────────────
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/

# ─── Environment / secrets ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.crt
credentials.json
.secrets
**/secrets.yml
**/secrets.yaml

# ─── Cloudflared ───────────────────────────────────────────────────────
# Tunnel credentials and host configs stay on the laptop.
# scripts/cloudflared-config.example.yml IS committed (template only).
.cloudflared/
*.cloudflared.json
cloudflared-config.yml

# ─── Model weights and large binaries ──────────────────────────────────
weights/*.pth
weights/*.pt
weights/*.ckpt
weights/*.onnx
weights/*.safetensors
*.pth.tar
weights/last.ckpt
weights/lightning_logs/

# Lightning / wandb / mlflow run artefacts
lightning_logs/
wandb/
mlruns/
outputs/

# ─── LiDAR / large 3-D data ────────────────────────────────────────────
*.laz
*.las
*.ply
*.pcd

# ─── Datasets ──────────────────────────────────────────────────────────
data/
datasets/
ttpla/
ttpla_raw/
**/__ttpla*/
*.h5
*.hdf5

# ─── App runtime ───────────────────────────────────────────────────────
uploads/
*.log
ktp-healthcheck.log

# ─── Evaluation outputs (regenerated by training/evaluate.py) ──────────
docs/evaluation_results.md
docs/evaluation_per_image.json
docs/screenshots/eval/

# ─── Notebooks ─────────────────────────────────────────────────────────
notebooks/*.ipynb
.ipynb_checkpoints/

# ─── IDE / editor ──────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ─── Editor / tool caches ──────────────────────────────────────────────
.deepeval/
.cursor/
.aider*
.continue/
.idx/
*.cache

# ─── Local working notes ───────────────────────────────────────────────
CLAUDE.md
NOTES.md
TODO.md
scratch/
docs/IMPLEMENTATION_BRIEF.md

# ─── OS / archive cruft ────────────────────────────────────────────────
*.tmp
*.bak
*.zip
*.tar.gz
*.tgz
*.rar

# ─── Docker ────────────────────────────────────────────────────────────
.docker/
docker-compose.override.yml

# ─── uv ────────────────────────────────────────────────────────────────
# uv.lock IS committed (lockfile-first policy — see docs/design_decisions.md).
.uv-cache/

# ─── Project-specific ──────────────────────────────────────────────────
# Operator-supplied example assets that are not in the repo by default.
# The synthetic_lv_*.jpg/.png pairs ARE committed (small, deterministic
# fixtures generated by scripts/synthesise_lv.py).
app/static/examples/ttpla_sample_*.jpg
app/static/examples/thatcham_sample.la[sz]
